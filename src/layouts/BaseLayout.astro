---
import '../styles/global.css';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';

interface Props {
  title?: string;
  description?: string;
  section?: 'life' | 'money' | 'culture' | 'tools' | 'dev' | 'vault';
  noindex?: boolean;
  ogImage?: string;
  lang?: 'ko' | 'en';
  alternateUrl?: string;
}

const {
  title = 'eone blog',
  description = '일상, 경제, 문화, 개발, 도구에 관한 이야기',
  section,
  noindex = false,
  ogImage,
  lang = 'ko',
  alternateUrl,
} = Astro.props;

const siteUrl = import.meta.env.PUBLIC_SITE_URL || 'https://blog.eone.one';
const canonicalUrl = Astro.url?.pathname
  ? new URL(Astro.url.pathname, siteUrl).toString()
  : siteUrl;
const ogImageUrl = ogImage ? new URL(ogImage, siteUrl).toString() : `${siteUrl}/og-default.png`;
const fullTitle = title === 'eone blog' ? title : `${title} — eone blog`;
---

<!doctype html>
<html lang={lang} class="antialiased">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />

    <title>{fullTitle}</title>
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl} />

    {noindex && <meta name="robots" content="noindex, nofollow" />}
    <link rel="alternate" hreflang={lang} href={canonicalUrl} />
    {alternateUrl && <link rel="alternate" hreflang={lang === 'ko' ? 'en' : 'ko'} href={new URL(alternateUrl, siteUrl).toString()} />}
    {alternateUrl && <link rel="alternate" hreflang="x-default" href={canonicalUrl} />}

    <!-- OG / Social -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={fullTitle} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonicalUrl} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:locale" content={lang === 'ko' ? 'ko_KR' : 'en_US'} />
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={fullTitle} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImageUrl} />

    <!-- Pretendard (variable font, jsDelivr CDN) -->
    <link
      rel="stylesheet"
      as="style"
      crossorigin
      href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable.min.css"
    />
    <!-- JetBrains Mono (code) -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@100..800&display=swap"
      rel="stylesheet"
    />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />

    <!-- Dark mode init (blocking — prevents flash) -->
    <script is:inline>
      (function () {
        const stored = localStorage.getItem('theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        if (stored === 'dark' || (!stored && prefersDark)) {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>

    <!-- Search Console 인증 -->
    {import.meta.env.PUBLIC_GOOGLE_SITE_VERIFICATION && (
      <meta name="google-site-verification" content={import.meta.env.PUBLIC_GOOGLE_SITE_VERIFICATION} />
    )}
    <!-- 네이버 서치어드바이저 인증 -->
    {import.meta.env.PUBLIC_NAVER_SITE_VERIFICATION && (
      <meta name="naver-site-verification" content={import.meta.env.PUBLIC_NAVER_SITE_VERIFICATION} />
    )}

    <!-- 페이지별 추가 head 슬롯 -->
    <slot name="head" />

    <!-- JSON-LD: WebSite -->
    <script is:inline type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "WebSite",
      "name": "eone blog",
      "url": siteUrl,
      "description": description,
      "inLanguage": lang === 'ko' ? 'ko-KR' : 'en-US',
    })} />

    <!-- Google AdSense -->
    {import.meta.env.PUBLIC_ADSENSE_CLIENT && (
      <script
        is:inline
        async
        src={`https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=${import.meta.env.PUBLIC_ADSENSE_CLIENT}`}
        crossorigin="anonymous"
      />
    )}

    <!-- Google Analytics -->
    {import.meta.env.PUBLIC_GA_ID && (
      <>
        <script is:inline async src={`https://www.googletagmanager.com/gtag/js?id=${import.meta.env.PUBLIC_GA_ID}`}></script>
        <script is:inline define:vars={{ gaId: import.meta.env.PUBLIC_GA_ID }}>
          window.dataLayer = window.dataLayer || [];
          function gtag() { dataLayer.push(arguments); }
          gtag('js', new Date());
          gtag('config', gaId);
        </script>
      </>
    )}
  </head>
  <body data-section={section}>
    <Header lang={lang} alternateUrl={alternateUrl} currentSection={section} />
    <main id="main-content">
      <slot />
    </main>
    <Footer />
  </body>
</html>
